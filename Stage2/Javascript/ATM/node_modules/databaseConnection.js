var mysql = require('mysql2');

var con = mysql.createConnection({
  host: "localhost",
  user: "abdalghany",
  password: "ohitsme",
  database:"atm"
});

function fetchPasswordFromDb(userAccountNo){
con.connect(function(err) {
  if (err) throw err;
  console.log("Connected!");
  con.query("SELECT customerpin FROM customers where customeraccountno = "+userAccountNo, function (err, result, fields) {
    if (err) throw err;
    if(result && result != ""){
      return (result[0].customerpin);
    }else{
      return "User does not exist";
    }
  });
});
}
console.log(fetchPasswordFromDb("158888899999"));